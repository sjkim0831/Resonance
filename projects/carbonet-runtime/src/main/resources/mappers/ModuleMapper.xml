<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.com.platform.module.dao.ModuleDao">

    <select id="selectModuleList" parameterType="egovframework.com.platform.module.vo.ModuleVO" resultType="egovframework.com.platform.module.vo.ModuleVO">
        SELECT id, name, version, status, owner, description, source
        FROM CARR_MODULE
        <where>
            <if test="source != null and source != '' and source != 'all'">
                AND source = #{source}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE '%' || #{keyword} || '%'
                     OR description LIKE '%' || #{keyword} || '%'
                     OR id LIKE '%' || #{keyword} || '%')
            </if>
        </where>
        ORDER BY status DESC, name
    </select>

    <select id="selectModuleById" parameterType="String" resultType="egovframework.com.platform.module.vo.ModuleVO">
        SELECT id, name, version, status, owner, description, source
        FROM CARR_MODULE
        WHERE id = #{id}
    </select>

    <insert id="insertModule" parameterType="egovframework.com.platform.module.vo.ModuleVO">
        INSERT INTO CARR_MODULE (id, name, version, status, owner, description, source)
        VALUES (#{id}, #{name}, #{version}, #{status}, #{owner}, #{description}, #{source})
    </insert>

    <update id="updateModule" parameterType="egovframework.com.platform.module.vo.ModuleVO">
        UPDATE CARR_MODULE
        SET name = #{name}, version = #{version}, status = #{status},
            owner = #{owner}, description = #{description}, source = #{source}
        WHERE id = #{id}
    </update>

    <delete id="deleteModule" parameterType="String">
        DELETE FROM CARR_MODULE WHERE id = #{id}
    </delete>

</mapper>
