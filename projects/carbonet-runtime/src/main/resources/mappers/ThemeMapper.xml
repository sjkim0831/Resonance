<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.com.platform.theme.dao.ThemeDao">

    <select id="selectThemeList" parameterType="egovframework.com.platform.theme.vo.ThemeVO" resultType="egovframework.com.platform.theme.vo.ThemeVO">
        SELECT id, name, version, status, owner, description, source
        FROM CARR_THEMA
        <where>
            <if test="source != null and source != '' and source != 'all'">
                AND source = #{source}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE '%' || #{keyword} || '%'
                     OR description LIKE '%' || #{keyword} || '%'
                     OR id LIKE '%' || #{keyword} || '%')
            </if>
        </where>
        ORDER BY status DESC, name
    </select>

    <select id="selectThemeById" parameterType="String" resultType="egovframework.com.platform.theme.vo.ThemeVO">
        SELECT id, name, version, status, owner, description, source
        FROM CARR_THEMA
        WHERE id = #{id}
    </select>

    <insert id="insertTheme" parameterType="egovframework.com.platform.theme.vo.ThemeVO">
        INSERT INTO CARR_THEMA (id, name, version, status, owner, description, source)
        VALUES (#{id}, #{name}, #{version}, #{status}, #{owner}, #{description}, #{source})
    </insert>

    <update id="updateTheme" parameterType="egovframework.com.platform.theme.vo.ThemeVO">
        UPDATE CARR_THEMA
        SET name = #{name}, version = #{version}, status = #{status},
            owner = #{owner}, description = #{description}, source = #{source}
        WHERE id = #{id}
    </update>

    <delete id="deleteTheme" parameterType="String">
        DELETE FROM CARR_THEMA WHERE id = #{id}
    </delete>

</mapper>
