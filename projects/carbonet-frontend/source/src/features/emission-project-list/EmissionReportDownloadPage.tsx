import {useEffect,useState} from "react";
import {buildLocalizedPath,isEnglish} from "../../lib/navigation/runtime";
type Report={id:number;version:number;title:string;language:string;status:string;certificateId?:string;integrityHash?:string;issuedAt?:string;downloadCount:number};
export function EmissionReportDownloadPage(){const en=isEnglish(),id=new URLSearchParams(location.search).get("projectId")||"",base=buildLocalizedPath(`/home/api/emission-projects/${id}/reports`,`/en/home/api/emission-projects/${id}/reports`);const[reports,setReports]=useState<Report[]>([]),[message,setMessage]=useState("");async function load(){const r=await fetch(base,{credentials:"include"}),b=await r.json();if(!r.ok)throw new Error(b.message);setReports(b.reports||[])}useEffect(()=>{if(id)void load().catch(e=>setMessage(e.message))},[id]);async function issue(r:Report){const x=await fetch(`${base}/${r.id}/issue`,{method:"POST",credentials:"include"}),b=await x.json();if(!x.ok)throw new Error(b.message);setMessage(en?"Certificate issued.":"인증번호를 발급했습니다.");await load()}async function download(r:Report){await fetch(`${base}/${r.id}/download`,{method:"POST",credentials:"include"});window.print();await load()}return <main className="min-h-screen bg-[#f5f7fa] px-4 py-10"><div className="mx-auto max-w-6xl"><a className="font-bold text-blue-700" href={buildLocalizedPath(`/emission/report_submit?projectId=${id}`,`/en/emission/report_submit?projectId=${id}`)}>← {en?"Report finalization":"보고서 확정"}</a><h1 className="mt-5 text-3xl font-black text-[#052b57]">{en?"Reports & Certificates":"보고서·인증서 다운로드"}</h1><p className="mt-2 text-slate-600">{en?"Issue a verifiable certificate and download the finalized report.":"확정 보고서에 인증번호를 발급하고 다운로드·진위 확인합니다."}</p>{message&&<p className="mt-5 rounded-lg bg-blue-50 p-4 font-bold text-blue-900">{message}</p>}<div className="mt-6 space-y-4">{reports.filter(r=>r.status==="FINALIZED").map(r=><article className="rounded-xl border bg-white p-6" key={r.id}><div className="flex flex-col justify-between gap-4 md:flex-row"><div><p className="font-bold text-blue-700">V{r.version} · {r.language.toUpperCase()}</p><h2 className="mt-1 text-xl font-black">{r.title}</h2><p className="mt-3 text-sm text-slate-600">{en?"Certificate":"인증번호"}: <strong>{r.certificateId||"-"}</strong></p>{r.integrityHash&&<code className="mt-2 block max-w-2xl break-all rounded bg-slate-100 p-2 text-xs">SHA-256 {r.integrityHash}</code>}<p className="mt-2 text-xs text-slate-500">{en?"Downloads":"다운로드"}: {r.downloadCount||0}</p></div><div className="flex flex-wrap items-start gap-2">{!r.certificateId&&<button className="rounded-lg bg-[#246beb] px-4 py-3 font-bold text-white" onClick={()=>void issue(r).catch(e=>setMessage(e.message))}>{en?"Issue certificate":"인증번호 발급"}</button>}<button className="rounded-lg border px-4 py-3 font-bold" onClick={()=>void download(r)}>{en?"Print / Save PDF":"인쇄·PDF 저장"}</button>{r.certificateId&&<a className="rounded-lg border border-green-700 px-4 py-3 font-bold text-green-800" href={`/report/verify?certificateId=${encodeURIComponent(r.certificateId)}`}>{en?"Verify":"진위 확인"}</a>}</div></div></article>)}</div></div></main>}
