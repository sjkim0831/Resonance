import{useEffect,useState}from"react";import{useAsyncValue}from"../../app/hooks/useAsyncValue";import{fetchHomePayload}from"../../lib/api/appBootstrap";import{buildLocalizedPath,isEnglish}from"../../lib/navigation/runtime";import{HeaderBrand,HeaderDesktopNav,HomeInlineStyles}from"../home-entry/HomeEntrySections";import{LOCALIZED_CONTENT}from"../home-entry/homeEntryContent";type Task={code:string;name:string;status:string;rule:string;completedAt?:string;completedBy?:string;targetUrl:string};type Data={project:{id:string;name:string;site:string;period:string;scope:string;progress:number;status:string};metrics:{activityCount:number;qualityPassCount:number;approvedSubmissions:number;totalEmission:number;finalizedReports:number;activeCertificates:number;accessCount:number};checklist:Task[];actors:{actorCode:string;userId:string;active:string}[];reports:{id:number;version:number;title:string;status:string;certificateId?:string;certificateStatus?:string;downloadCount:number}[]};function Completion({admin=false}:{admin?:boolean}){const en=isEnglish(),home=useAsyncValue(()=>fetchHomePayload(),[en]),id=new URLSearchParams(location.search).get("projectId")||"PRJ-2026-001",[d,setD]=useState<Data|null>(null),[msg,setMsg]=useState("");useEffect(()=>{fetch(buildLocalizedPath(`/home/api/emission-projects/${id}/completion`,`/en/home/api/emission-projects/${id}/completion`),{credentials:"include"}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.message);setD(b)}).catch(e=>setMsg(e.message))},[id]);const m=d?.metrics;return <><HomeInlineStyles en={en}/><div className="min-h-screen bg-slate-50">{admin?<header className="border-b-2 border-[#052b57] bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5"><strong className="text-xl text-[#052b57]">CCUS 관리자</strong><a href="/admin" className="font-bold text-blue-700">관리자 홈</a></div></header>:<header className="border-b-2 border-[#001e40] bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center px-4"><HeaderBrand content={LOCALIZED_CONTENT[en?"en":"ko"]} en={en}/><HeaderDesktopNav en={en} homeMenu={home.value?.homeMenu||[]}/></div></header>}<main className="mx-auto max-w-7xl px-4 py-8"><p className="font-bold text-blue-700">{d?.project.site||id}</p><div className="mt-1 flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><h1 className="text-3xl font-black text-[#052b57]">{admin?"프로젝트 완료 관리":"프로젝트 완료 대시보드"}</h1><p className="mt-2 text-slate-600">{d?.project.name} · {d?.project.period} · {d?.project.scope}</p></div><a className="rounded-lg bg-blue-700 px-5 py-3 font-bold text-white" href={`/emission/project/renew?sourceProjectId=${id}`}>다음 보고연도 프로젝트 만들기</a></div>{msg&&<p className="mt-5 rounded bg-red-50 p-4 text-red-800">{msg}</p>}<section className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["진행률",`${d?.project.progress||0}%`],["총배출량",`${Number(m?.totalEmission||0).toLocaleString()} tCO₂e`],["최종 보고서",m?.finalizedReports||0],["인증서·접근",`${m?.activeCertificates||0} / ${m?.accessCount||0}`]].map(([l,v])=><div className="rounded-xl border bg-white p-5" key={l}><p className="text-sm font-bold text-slate-500">{l}</p><strong className="mt-2 block text-2xl text-[#052b57]">{v}</strong></div>)}</section><section className="mt-6 rounded-xl border bg-white p-6"><h2 className="text-xl font-black text-[#052b57]">완료 체크리스트</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{d?.checklist.map(t=><a href={t.targetUrl||"#"} className="rounded-lg border p-4 hover:border-blue-500" key={t.code}><div className="flex justify-between"><strong>{t.name}</strong><span className={`rounded-full px-2 py-1 text-xs font-black ${t.status==="DONE"?"bg-green-100 text-green-800":"bg-amber-100 text-amber-900"}`}>{t.status}</span></div><p className="mt-2 text-sm text-slate-600">{t.rule}</p><small className="mt-2 block text-slate-500">{t.completedBy||"-"} {t.completedAt&&`· ${new Date(t.completedAt).toLocaleString()}`}</small></a>)}</div></section><div className="mt-6 grid gap-5 lg:grid-cols-2"><section className="rounded-xl border bg-white p-6"><h2 className="text-xl font-black">참여 액터</h2>{d?.actors.map(a=><div className="mt-3 flex justify-between border-b pb-2" key={a.actorCode+a.userId}><strong>{a.actorCode}</strong><span>{a.userId}</span></div>)}</section><section className="rounded-xl border bg-white p-6"><h2 className="text-xl font-black">최종 산출물</h2>{d?.reports.map(r=><div className="mt-3 rounded-lg border p-3" key={r.id}><strong>V{r.version} · {r.title}</strong><p className="mt-1 text-sm">{r.status} · {r.certificateId||"인증번호 미발급"} · 다운로드 {r.downloadCount||0}회</p></div>)}</section></div></main></div></>};export function UserProjectCompletionPage(){return <Completion/>}export function AdminProjectCompletionPage(){return <Completion admin/>}
