<dependencies>
    <dependency>
        <groupId>egovframework</groupId>
        <artifactId>screenbuilder-core</artifactId>
        <version>${screenbuilder.version}</version>
    </dependency>
    <dependency>
        <groupId>egovframework</groupId>
        <artifactId>screenbuilder-runtime-common-adapter</artifactId>
        <version>${screenbuilder.version}</version>
    </dependency>

    <!-- Optional shared modules. Keep only the ones the project adapter truly needs. -->
    <dependency>
        <groupId>egovframework</groupId>
        <artifactId>carbonet-web-support</artifactId>
        <version>${screenbuilder.version}</version>
    </dependency>
    <dependency>
        <groupId>egovframework</groupId>
        <artifactId>carbonet-contract-metadata</artifactId>
        <version>${screenbuilder.version}</version>
    </dependency>
    <dependency>
        <groupId>egovframework</groupId>
        <artifactId>carbonet-builder-observability</artifactId>
        <version>${screenbuilder.version}</version>
    </dependency>

    <!-- Replace with the new project adapter artifact. -->
    <dependency>
        <groupId>com.example</groupId>
        <artifactId>screenbuilder-project-adapter</artifactId>
        <version>${project.version}</version>
    </dependency>
</dependencies>
