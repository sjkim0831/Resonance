# Adapter Jar Placement

Place the project-owned adapter jar here.

Expected default filename:

- `project-adapter.jar`

This jar should contain only:

- project binding implementations
- project DB mapping
- project route/menu/theme binding
- project-specific executor bridges

Do not copy common-core source into this folder.
