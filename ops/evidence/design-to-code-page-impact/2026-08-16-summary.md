# Design-to-code page impact ledger — 2026-08-16

## Audit boundary

- Base Git SHA: `873d28a62c36e2a5e97183178d7aa6d18019577c`
- Live database label: `carbonet-production-key-only-read-only-union-v1`
- Live execution: `2026-08-16T11:12:37Z`, `131,828 ms` of the `600,000 ms` review budget
- Core union query: recorded warm run `3,468 ms`; separate cold verification `8,882 ms`
- Database guard: explicit read-only transaction, statement timeout `300,000 ms`, lock timeout `5,000 ms`
- Mutations: live database writes `0`, live file writes `0`
- Selector mode: `ALL_COMPILER_READY_MAXIMUM`; selector-free count is a maximum, not the result of editing one named screen or shared asset

## Authoritative identity correction

The executable screen identity is now the distinct union of:

1. `requires_user_page=true` and `requires_admin_page=true` direct step routes; and
2. every route referenced by an `ACTIVE` `framework_process_step_screen_binding`.

The exact key remains `upper(process_code) | upper(step_code) |
upper(audience) | lower(path-before-query)`. Direct, active, overlap, direct-only,
and active-only counts remain separately visible. This closes the former blind
spot where an active auxiliary screen was excluded merely because it was not the
step's one direct `user_path` or `admin_path`.

## Live counts

| Boundary | Count |
|---|---:|
| Physical screen-resource rows / normalized routes | 1,951 / 1,951 |
| Direct required exact identities | 1,043 |
| Active-binding exact identities | 1,222 |
| Direct ∩ active overlap | 1,029 |
| Authoritative union | 1,236 |
| Direct-only / active-only identities | 14 / 193 |
| Multi-route step+audience groups / identities / additional routes | 39 / 182 / 143 |
| Resource exact / missing / duplicate | 1,230 / 6 / 0 |
| Contract target / matched / missing | 1,236 / 1,179 / 57 |
| Professional contract unique exact / duplicate | 1,144 / 35 |
| VALID blueprint exact / missing / physical duplicate | 986 / 207 / 43 |
| Duplicate blueprint authority resolved / unresolved | 35 / 8 |
| Exact resource + contract + single blueprint | 934 |
| Generated / hybrid / manual / ambiguous / unclassified ownership | 823 / 163 / 35 / 8 / 207 |
| Compiler-ready / source-emitted-ready | 901 / 901 |
| SOURCE catalog eligible exact identities | 901 |
| Expected endpoint screens / operations | 1,140 / 4,788 |
| Generated endpoint artifacts / Java sources | 0 / 0 |
| KRDS / SDUI-or-JSON-Form identities | 1,021 / 0 |
| Common-asset ready / missing active pages | 565 / 0 |
| Incomplete / ambiguous exact identities | 335 / 43 |

The union arithmetic is exact: `1,043 + 1,222 - 1,029 = 1,236`.
Contract reconciliation is also closed arithmetically: `1,144` unique matches
plus `35` duplicate matches plus `57` missing = `1,236` targets.

## Performance correction

The first union implementation was safely cancelled after one read-only query
reached `197 s`. The repeated source-membership joins were replaced by one
grouped provenance flag, and blueprint contract authority changed from a
correlated scan to a hash-joinable authority-reference set. The cold core query
then completed in `8,882 ms` (`95.5%` reduction), and the recorded live core
probe completed in `3,468 ms`.

The SOURCE readiness aggregate is independently capped at `30,000 ms`; it can no
longer consume the whole ten-minute audit budget before the bounded per-process
isolation path runs. The full live ledger completed in `131,828 ms`.

## Repository closure

- Canonical Korean routes: 308; route source files: 15.
- Page manifests: 231; route-bound manifests: 231.
- Generated catalog imports and declared definitions: 1,378 each.
- Tracked/materialized generated definition files: 0/0.
- KRDS CSS tokens: 22; SDUI/JSON-Form reference files: 9.
- System component catalog entries: 0.

## Truthful blockers

The live status remains `INCOMPLETE`. The main gaps are 57 missing and 35
duplicate target contracts, 207 missing and 8 unresolved duplicate blueprints,
335 incomplete authoritative identities, 1,378 untracked generated definitions,
1,140 expected endpoint screens with 0 tracked artifacts, and an empty tracked
common-component catalog.

The aggregate SOURCE readiness probe truthfully reports its 30-second timeout.
The independent bounded sample covered 64 of 139 processes: 42 succeeded, 22
failed, and 75 were omitted by the existing safety cap. No failure or omission
is converted to zero or success.

## Selector and schema evidence

The isolated PostgreSQL fixture validates seven cases in `29,017 ms` with
database writes `0`:
global union (`direct=4`, `active=2`, `overlap=1`, `union=5`), process selector
(`4` identities), direct exact selector (`1`), active-only auxiliary exact
selector (`1`), recursive COMPONENT asset DAG fanout (`3`), isolated readiness
failure, and truthful old-schema failures. The fixture ledger and live ledger
both pass the Draft 2020-12 JSON schema.

The next rock is to close the 193 active-only identities through the same
contract, blueprint, generated frontend/backend, endpoint, and process-test
pipeline; direct route coverage alone is no longer accepted as completion.
