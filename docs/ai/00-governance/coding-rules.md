# Coding Rules

Document the project-specific implementation rules that AI should not guess.

Suggested contents:

- package layout rules
- template naming rules
- Korean and English parity rules
- mapper and XML namespace rules
- menu code and feature code handling
- React parity rules for migrated pages
- file naming and location rules for SQL
- `.gitignore` maintenance rules for local-only artifacts, generated assets, runtime logs, env files, and caches

Add only project-specific rules here. Do not restate generic style-guide advice.
