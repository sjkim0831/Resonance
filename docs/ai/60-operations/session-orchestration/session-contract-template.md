# Session Contract Template

- `requestId`:
- `taskGroupId`:
- `ownerSession`:
- `role`:
- `goal`:

## Boundaries

- `allowedPaths`:
- `forbiddenPaths`:
- `sharedFilesOwnedByOthers`:

## Inputs

- `inputContracts`:
- `dependencies`:
- `requiredDocs`:

## Outputs

- `expectedFiles`:
- `expectedBehavior`:
- `docsToUpdate`:

## Done Criteria

- implementation complete:
- no unauthorized shared-file edits:
- contract respected:
- handoff note prepared:

## Handoff

- `handoffTo`:
- `handoffCondition`:
