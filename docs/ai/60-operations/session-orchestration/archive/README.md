# Archived Session Plans

Move completed session-planning artifacts here when they remain useful as references.

If an artifact is broadly reusable, prefer also adding a condensed example under:

- `docs/ai/70-reference/sample-change-specs/`
